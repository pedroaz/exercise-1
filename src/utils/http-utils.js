
const axios = require('axios');

// Get the json data from an url
module.exports = async function getDataFromUrl(url) {
  
  // Use Axios to make a http call to the url
  // Use async / await pattern to get the http response

  // Return the data from the response object
}