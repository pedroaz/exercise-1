
const fs = require('fs');

module.exports = class InputService{

    // Constructor of the class
    constructor(filePath){
        this.filePath = filePath;
    }

    // Read Config File and return its data
    readApplicationInputs(){

        // Use fs library to read the config file
        
        // Conver file to json data and return it
    }
}